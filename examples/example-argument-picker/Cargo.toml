[package]
name = "example-argument-picker"
version = "0.1.0"
edition = "2024"

[dependencies.mingling]
path = "../../mingling"

# Enable `picker` features
features = ["picker", "extra_macros"]

[workspace]
