[package]
name = "example-combine-pathf-dispatch-tree"
version = "0.1.0"
edition = "2024"

[dependencies]
mingling = { path = "../../mingling", features = [
    "dispatch_tree",
    "pathf",
] }

[workspace]
