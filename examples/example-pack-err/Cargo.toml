[package]
name = "example-pack-err"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }

[dependencies.mingling]
path = "../../mingling"
features = [
    "general_renderer",
    "extra_macros",
]

[workspace]
